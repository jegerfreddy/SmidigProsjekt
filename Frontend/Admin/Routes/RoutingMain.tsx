import { BrowserRouter, Link, Route, Routes } from "react-router-dom";
import React from "react";

const RoutingMain = () => {

    return (
        <>
            <BrowserRouter>
                <nav>

                </nav>
            </BrowserRouter>
        </>
    )
}