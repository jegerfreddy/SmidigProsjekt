import { FC } from "react";
import { IEventItem } from "../Interfaces/IEventItem";

const EventItem : FC<IEventItem> = ({id, actID, eventTitle, option1, option2, option3, option4}) => {

    return (
        <div>

        </div>
    );
}

export default EventItem;